# API Endpoints (REST)

All endpoints must be scoped by account_id.

**Core Endpoints**:
/units
/tenants
/leases
/transactions
/maintenance-requests
/documents (with signed S3 URLs)
/form-letters
/rent-roll
/general-ledger
/stripe/webhook
/tenant-portal/login (separate auth flow)