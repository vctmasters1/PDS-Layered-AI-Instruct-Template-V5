# Tool: get_applicable_rules

Description: Retrieves scoped curriculum, privacy, and policy rules for a specific context.

Parameters:
- state (required)
- district (required)
- school (optional)
- subject (optional)

This tool ensures every downstream agent only sees the relevant rules.