This directory holds temporary output files: boot logs, capture dumps, diff outputs, test results, etc.

Rules:
- Contents are ephemeral — may be deleted at any time
- Never place temporary files in the workspace root — always put them here
- Do not commit contents of this directory (covered by .gitignore)
