# Resume Suite Connection Info — LOCAL DEVELOPMENT MODE

**Date**: 2026-06-03  
**Mode**: LOCAL DEVELOPMENT  
**Deployment**: None (pure development)

---

## URLs

| Component | URL |
|-----------|-----|
| Client (Vite Dev Server) | http://localhost:5173 |
| API Server (Express) | http://localhost:38291 |
| Database | postgres://localhost:5433 |

---

## Access

- **Local only**: These services are only accessible from this machine
- **No TLS**: HTTP only (development convenience)
- **Auto-reload**: Vite and Express both use file watchers for live reload

---

## Environment

- `DEPLOY_MODE`: local
- `NODE_ENV`: development
- Database user: `resumesuite`
- Database password: `devpassword` (dev only)
- LLM API: Assumes LM Studio running on `localhost:1234`

---

## Start / Stop

```powershell
# Start
cd K:\PDS-Master-001\WEB-Resume\ResumeServer
./start-dev-modes.ps1 -Mode local

# Stop: Ctrl+C in any terminal window (all services stop together)
```

---

## Logs

- Vite: Terminal window 1 (shows compilation status)
- Express: Terminal window 2 (shows API requests)
- PostgreSQL: Part of Docker stack (see with: `docker compose logs -f db`)

---

## Troubleshooting

**Vite stuck or not updating**
```bash
# Kill existing process
lsof -ti:5173 | xargs kill -9  # macOS/Linux
netstat -ano | findstr :5173   # Windows (find PID, then taskkill /PID)

# Restart
npm run dev
```

**Express won't start**
```bash
# Check if port is in use
netstat -ano | findstr :38291

# Install dependencies
npm install
```

**Database connection refused**
```bash
# Ensure Docker is running
docker ps

# Start database manually
docker compose -f docker-compose.dev.yml up -d db

# Wait a few seconds for PostgreSQL to initialize
```

---

## See Also

- [DEPLOYMENT-MODES.md](DEPLOYMENT-MODES.md) — Full deployment mode guide
- [.env.example](../.env.example) — Environment configuration
