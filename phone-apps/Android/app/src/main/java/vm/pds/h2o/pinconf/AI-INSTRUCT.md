This directory contains an abstract and flexible implimentation of pin configuration.

These classes and routines should first look at dev_platforms/abstract
Then dev_platforms/{platform}/{common}
then instide the {hwrev}{role}

there shouldnt be a bunch of duplicated code