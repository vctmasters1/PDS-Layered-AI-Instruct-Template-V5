{
  "name": "auth-api-example",
  "version": "0.1.0",
  "private": true,
  "description": "Example module — illustrates the layering rules in .ai/instruct.md. Not a runnable service.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint:layering": "node ./scripts/check-layering.mjs"
  },
  "dependencies": {
    "argon2": "^0.31.0",
    "express": "^4.19.0",
    "jsonwebtoken": "^9.0.0",
    "pino": "^9.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/jsonwebtoken": "^9.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
